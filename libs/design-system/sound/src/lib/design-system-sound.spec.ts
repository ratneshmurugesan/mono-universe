import { designSystemSound } from './design-system-sound';

describe('designSystemSound', () => {
  it('should work', () => {
    expect(designSystemSound()).toEqual('design-system-sound');
  });
});
