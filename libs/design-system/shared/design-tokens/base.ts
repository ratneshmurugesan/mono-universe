import * as core from './core.json';
export { core };

// type color = string;
// type font = string; // alias type, typography, and text
// type size = string; //alias sizing
// type space = string; // alias units, dimension, and spacing
// type duration = string; // alias animation, transition and time
// type elevation = string; // alias z-index and layer(ing)
// type breakpoint = string; //alias media-query, responsive
// type shadow = string; // alias depth
// type touch = string;
// type border = string;
