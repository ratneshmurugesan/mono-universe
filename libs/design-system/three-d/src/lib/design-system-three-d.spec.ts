import { designSystemThreeD } from './design-system-three-d';

describe('designSystemThreeD', () => {
  it('should work', () => {
    expect(designSystemThreeD()).toEqual('design-system-three-d');
  });
});
