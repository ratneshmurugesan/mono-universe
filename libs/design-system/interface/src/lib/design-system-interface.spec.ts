import { designSystemInterface } from './design-system-interface';

describe('designSystemInterface', () => {
  it('should work', () => {
    expect(designSystemInterface()).toEqual('design-system-interface');
  });
});
