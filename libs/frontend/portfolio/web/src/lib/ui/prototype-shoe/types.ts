export type TModelState = {
  current: null
  items: {
    laces: string
    mesh: string
    caps: string
    inner: string
    sole: string
    stripes: string
    band: string
    patch: string
  }
}
