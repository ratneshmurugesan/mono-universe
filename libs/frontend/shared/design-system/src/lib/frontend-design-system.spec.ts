import { frontendDesignSystem } from './frontend-design-system'

describe('frontendDesignSystem', () => {
  it('should work', () => {
    expect(frontendDesignSystem()).toEqual('frontend-design-system')
  })
})
