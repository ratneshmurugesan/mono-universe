import { sharedStyledSystem } from './shared-styled-system';

describe('sharedStyledSystem', () => {
  it('should work', () => {
    expect(sharedStyledSystem()).toEqual('shared-styled-system');
  });
});
